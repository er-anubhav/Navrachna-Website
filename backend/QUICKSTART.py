"""
NFED Backend — Quick Start
===========================
1. Make sure Python 3.11+ is installed
2. cd into this folder:  cd backend
3. Create a venv:        python -m venv .venv
4. Activate it:
   Windows:  .venv\\Scripts\\activate
   Mac/Linux: source .venv/bin/activate
5. Install deps:         pip install -r requirements.txt
6. Run the server:       uvicorn main:app --reload --port 8000

The SQLite database (nfed.db) will be auto-created on first launch.
Swagger docs: http://localhost:8000/api/docs
"""
