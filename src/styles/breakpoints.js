// Breakpoint pixel values — used in Admin canvas preview toggle
// These match Tailwind v4 default breakpoints (sm/md/lg/xl)
export const BREAKPOINTS = {
  mobile:  375,
  tablet:  768,
  desktop: 1024,
  wide:    1440,
}

// Preview container widths for the Admin page builder canvas
export const PREVIEW_WIDTHS = {
  mobile:  '375px',
  tablet:  '768px',
  desktop: '100%',
}
